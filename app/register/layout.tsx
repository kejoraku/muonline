import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Mu Isekai | Register',
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
