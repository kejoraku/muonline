import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Mu Isekai | Rankings',
};

export default function RankingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
