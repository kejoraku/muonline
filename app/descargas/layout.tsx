import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Mu Isekai | Descargas',
};

export default function DescargasLayout({ children }: { children: React.ReactNode }) {
  return children;
}
