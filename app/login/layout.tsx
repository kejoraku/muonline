import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Mu Isekai | Log In',
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
